---
name: parl-monitor-build
description: "CitizenGO Parliamentary Monitor build state, fixtures origin, and open spec questions"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2fa51e3b-34ec-4638-9e48-df2232ac2520
  modified: 2026-08-31T20:05:30.080Z
---

Weekly UK parliamentary monitoring pipeline. Repo root: `~/parl-monitor`, but **primary runtime is GitHub Actions** (private repo `thejoycething-code/parl-monitor`, migrated 2026-08-03, test run green). Laptop = dev + manual fallback; launchd retired (plists in tools/launchd-retired/). State (db/raw/editions/reviews) is committed by workflow runs — **always `git pull` before local work**. Actions secrets: ANTHROPIC_API_KEY + ASANA_PAT set; SLACK_BOT_TOKEN pending admin approval of the bot app (until then Slack post = in-session as Chris, weekly go-ahead). Deploy key `github.com-parlmonitor` host alias on laptop. Costs: ~120 Actions min/month of 2000 free; ~£2/yr Claude API. Spec is `docs/claude-code-handoff-parl-monitor.md`; build order + two human checkpoints in `CLAUDE.md`. Python 3.9.6 on this machine but spec wants 3.11+ (code kept 3.9-runnable). stdlib-only (urllib/sqlite3); pyyaml/httpx not installed.

**All Phase 1 steps (1-7) built; first edition generated.** Done: `src/http.py`, `src/db.py`, `src/ingest/bills.py`, `src/board.py`, step-3 ingesters `src/members.py` + `src/ingest/{pqs,wms,edms,sis,divisions,whatson,consultations,legislation}.py`, step-4 `src/filter.py` (+ `config/taxonomy.yaml` v0.2 & `config/watchlist.yaml`), step-5 `src/triage.py` (stub + live-via-injectable-transport), step-6 `src/digest.py`, step-7 orchestrator `run_weekly.py`. pyyaml installed (--user). **74 passing tests.** Generated `editions/parliamentary-monitor-2026-08-03.md` (recess mode) from live-probed data.

**Review round-trip built (step 6 completion).** `src/review.py` + two-phase CLI: `run_weekly.py --pull <week>` ingests and emits `reviews/review-<week>.md` (PRIORITY/OWNER/WHY per item, blank PRIORITY = drop); `--render <week>` applies edits back to `items` and renders sections **from the store** via `sections_from_store()`; `--draft` does both with suggested tags. ACT-without-owner refuses to render (verified). 83 tests. Sample format reference lives at `docs/sample-edition.md`, regenerated by `tools/make_sample.py` (fabricated data, keep the SAMPLE banner).

**Unattended Sunday pull + session triage (decided 2026-08-02, no API key).** Christopher's Claude.ai subscription can't bill the API, so triage runs on his Claude Code entitlement instead: `TRIAGE=session` emits `reviews/triage-queue-<week>.md` (§7 rubric embedded); a Claude Code session fills SCORE/WHY; `--render` applies them (`run_weekly.apply_queue_if_scored`). Scores are no longer baked at ingest — items store with `triage_score NULL` and ALL matched items (incl. tier-2) reach the scoring pass; watchlist floor (min 2) enforced in `triage.apply_scores`; score-0 → `discards`. The deterministic pull is scheduled via **launchd** (`~/Library/LaunchAgents/net.citizengo.parlmonitor.pull.plist`, Sun 21:04, runs-on-wake) invoking `tools/sunday_pull.sh`, which targets the coming Monday and logs to `data/pull-logs/`. Verified live 2026-08-02. Monday flow: open queue file → score in session → edit review file → `--render`. 104 tests.

**Edition 1 (w/c 2026-08-03) finalised with Christopher's review:** Tying the Knot = ACT, **Owner: Zuzana** (she runs the weddings-law campaign); SEND/EOTAS held at NOTE pending a decision on a supporter response. Re-render bug found and fixed: the one-closing-entry guard was per-render, not per-edition — re-rendering an edition lost its closing rows. `load_closed_bill_ids(conn, exclude_edition=week)` + same-edition `record_closure` is idempotent now.

**Format changes at Christopher's request (2026-08-02):** (a) recess status + return dates render once as a combined top line via `digest.recess_line()` (return dates now computed from What's On lookahead, not hardcoded); the mid-page banner and footer are gone. (b) Board gained a **"Why we track it"** column (between Bill and House-and-stage), sourced from `why:` fields in `config/watchlist.yaml` (`bills`, new `fallen_bills` block, `acts_watch` with `bill_id`, `holyrood`); board bill ids/areas/why all come from the watchlist now, not hardcoded dicts in run_weekly. (c) Movement markers are worded: NEW / "▲ moved" / "no change" / closing, plus a one-line key under the table (`digest.MOVEMENT_KEY`). Sample format reference (`tools/make_sample.py` → `docs/sample-edition.md`) updated to match; keep regenerating it whenever the format changes.

**2026-08-02 late session:** PQ/EDM sweeps live (config/settings.yaml terms; 'home education' + 'safe access zones' failed after retries exactly like the pilot — honest gaps footer proven). Two §9.5 PQ clusters captured live: age assurance + anti-Muslim hostility definition (4 PQs). Board snapshots persist (single continuous data/parl-monitor.db; per-week dbs dead). store_item is now an editorial-preserving upsert and review regeneration merge-preserves PRIORITY/OWNER/WHY — a re-pull can no longer wipe decisions (it did once; state restored by hand). Git live: commits db75551, 2bcb944; no remote yet (gh not installed — user to decide GitHub vs folder backup). Slack: weekly edition posts to #campaigns-en-gb (C9RH217PZ) as summary + Canvas; full-density fabricated template Canvas F0BM5E5GWR5 shared with Chris's coach-ride note. Asana: NO recurrence — a fresh task is created each Monday as step 5 of the Monday ritual (now in project CLAUDE.md "Weekly operations"), named "Read the Parliamentary Monitor — w/c <date>", assignee me, due that Monday, notes carrying that week's Canvas link + ACT items + 3-week deadlines + standing checklist. This week's task gid 1217111287958855 updated to that pattern. PQs don't render during recess (template rule: sections 1,2,6,7,11 only) — they surface when normal mode resumes.

**Decisions this session:** no pilot edition exists — generate from scratch, template is format guide only. Westminster-only (Holyrood/scotland.py deferred). Filter: acronym entities (CARE/PATHWAYS/RSE) match case-sensitively to avoid common-word false positives. Editions contain no em dashes (board "unchanged" marker is a hyphen).

**Holyrood added (2026-08-01), completing §9.2.** `src/ingest/scotland.py`: bills API is identity-only and now responds in ~5s (pilot saw 60s timeout); status comes only from scraping the bill page. Beware the page's "Current status:" lines, which belong to motions not the bill. Holyrood board rows use negative bill_ids (e.g. -445) since bills_board.bill_id is a shared int PK. 99 tests.

**Acceptance §9 from the generated edition:** 9.1 board dates ✓ (4157/4144/4178/4171), 9.2 all four closes ✓ (3774 fall, 3938/3909 RA, Holyrood fell 2026-03-17), 9.3 s.241/242 in force at RA ✓, 9.4 recess + return 2026-09-01 + TIA first on 2026-09-11 order paper ✓, 9.7 SI+division#51 entity link ✓, 9.8 SEND/EOTAS consultation ✓. NOT reproducible from live feed (disclosed as gaps in footer): 9.5 PQ clusters, 9.6 EDMs.

**Fixtures were absent on delivery** — no `data/raw/2026-08-01/`, no pilot edition. User chose to **live-probe** the keyless Bills API; real responses for bills 4157/3774/3938/3909 now archived in `data/raw/2026-08-01/`. Live data reproduced acceptance 9.1/9.2 for 4157 (next date 2026-09-11) and 3774 (fallen, session 39 vs current 40, 14 Lords cttee sittings to 2026-04-24).

**Decisions made (Christopher, 2026-08-01):**
- RA date: board closing entry uses the **parliamentary RA-stage date** (bills-api = 2026-04-29), NOT legislation.gov.uk commencement. So §9.2's 2026-05-11/06-26 won't reproduce on the board; the 11 May in-force date comes via the §9.3 legislation check (step 3). §9.2 board-date expectations should be updated.
- Cold-start closes: **Option B** — absolute terminal detection via `board.discover_closures()` + `closed_edition` guard (fire once ever). Implemented + tested.
- **Scope: Westminster only for now.** Holyrood fall detection (acceptance 9.2's 4th close) deferred to the Scotland ingester. Cold run currently surfaces 3 closures (3774, 3938, 3909), not 4.

**Still open before §9 acceptance / Checkpoint 2:**
- Triage model id `claude-sonnet-4-6` in spec §7 is invalid (use `claude-sonnet-5`); non-blocking under TRIAGE=stub.
- Human-authored pilot edition still needed for Checkpoint 2 side-by-side (cannot be regenerated by code).

**Full automation (option B) live as of 2026-08-03:** Sunday 21:04 pull (TRIAGE=auto: live scoring iff `config/secrets.yaml` has anthropic_api_key, else stub) + Monday 06:30 publish (`run_monday.py` via `net.citizengo.parlmonitor.monday`): render, Slack canvas + summary to #campaigns-en-gb via bot token, fresh Asana reading task via PAT. Missing credentials skip-with-disclosure. Unreviewed editions ship WATCH/NOTE-only (ACT needs human owner). **Slack bot app approval requested 2026-08-03 (workspace requires admin approval; manifest: chat:write + canvases:write).** Interim, per Chris: each Monday the Slack post is done in-session via the claude.ai connector as Chris, with his explicit per-week approval ("post it") — never post without that go-ahead. Still to drop into config/secrets.yaml: anthropic_api_key (else stub triage) and asana_pat (else no auto reading task); slack_bot_token arrives if/when admins approve.

**Committees CfE ingester live (2026-08-03, first Phase 2 feed).** §4.12's untested endpoint works: `CommitteeBusiness?CurrentlyAcceptingEvidence=true` (unfiltered listing returns 2005-era junk); deadline = `openSubmissionPeriods[0].endDate`, rolling calls have none; committee names only via `/api/Committees?CommitteeBusinessId={id}` (resolved for matched items only). First catch: Communications and Digital Committee's "Online Safety Act: implementation and impact" inquiry, evidence closes 2026-09-07, live-triaged score 3 — sits in reviews/review-2026-08-10.md as an ACT candidate needing an owner decision from Chris. OSA inquiry set ACT/owner Christopher in review-2026-08-10.md (applies at Monday's cloud render). Taxonomy regenerator done: docs/keyword-taxonomy.md is the v0.2 master with {#yaml_key} heading anchors + semicolon term lines; tools/generate_taxonomy.py regenerates config/taxonomy.yaml; tests/test_taxonomy_sync.py fails on drift. Edit md -> regenerate -> commit. Remaining roadmap (September, House returns 1 Sep): run_alerts.py, Hansard, first discards review; 

**Taxonomy v0.3 (2026-08-03, Chris approved):** 18 new terms — decriminalis* (A1), MAiD + medical assistance in dying (A2), social transition* (A3), EHRC + gender reassignment (A5), home schooling/homeschool* T1 + sex education + grooming gang* (A6), Islamophobia T1 + Islamis* (A7), religious freedom/liberty (A8), sharia (A9), embryo/polygenic screening + artificial womb* (A10). **Migration is now AREA 11** (Chris's decision, same day — reversing the earlier scoping): taxonomy v0.3 with tier-1 precision terms (small boats, asylum hotel*, net migration, Immigration and Asylum Bill...) and bare migration/immigration/asylum at tier 2; §7 triage prompt extended (deviation logged in api-notes); bill 4254 Immigration and Asylum Bill on the board (areas [11]); Border Security, Asylum and Immigration Act 2025 in acts_watch (no bill_id deliberately — 2025 assent predates monitor, no closing entry). Filter heuristic now case-sensitive for mixed-case acronyms (MAiD≠maid, FoRB≠forb); hyphenated (self-ID) still insensitive. PQ sweep += Islamophobia, grooming gangs, home schooling; EDM sweep += Islamophobia. 138 tests.

**Partner edition live (2026-08-03).** https://parl-monitor-partner.vercel.app — Vercel project parl-monitor-partner, citizen-go-teammates scope, prj_S8GXaZdccVrPLzoXQdWaiq1MyZGr. Content per Chris: tags + why-lines kept, owner fields stripped, owner names scrubbed from prose (src/partner.py redact + tests), section 11 dropped. Access: basic auth via edge middleware, user `partners`, passphrase `board-primrose-westminster-42` (PARTNER_PASSPHRASE env in Vercel; rotate there + redeploy). Vercel's team SSO protection was disabled for this project via API so the middleware gates instead. run_monday.py builds partner_site/ each week; Actions auto-deploys when a **VERCEL_TOKEN secret is added** (not yet — until then deploy in-session Monday: `vercel deploy partner_site --prod` from repo, scope citizen-go-teammates). Site html committed to repo as archive.

**Section order changed (Chris, 2026-08-03):** 1 Top lines, 2 Week ahead, 3 Votes, 4 PQs, 5 Committee, 6 Consultations/SI, 7 EDMs, 8 Devolved, 9 Statements, **10 Active bills board**, 11 MP intelligence. Recess mode: 1, 5, 6, 10, 11. Deviation from digest-template.md logged in api-notes. Partner site + sample regenerated; July edition reordered mechanically (its per-week db no longer exists).

**Deadline table (Chris's design, 2026-08-03 evening):** B2 layout + urgency chips. Sections: 1 Top, 2 Week ahead, 3 Votes, 4 PQs, **5 Consultations and calls for evidence** (merged committee+consultation, date-sorted 2-col table: bold **Consultation**/**Evidence** type marker before title, Closes cell = "Fri 4 Sep · 32 days"), 6 EDMs, 7 Devolved, 8 Statements, 9 Board, 10 MP notes. No ACT/WATCH/NOTE in the table (urgency belongs in Top lines). Partner site: type chips (filled/outlined blue) + Closes tinted **red ≤8 days, amber ≤21, grey beyond** (Chris's bands). SIs = note lines under table. Asana deadlines from store (deadlines_from_store). **Redaction lesson:** scrub list must come from items.owner via partner.owners_from_store — the table has no Owner fields so page-derived names silently vanished and Zuzana leaked to the live site briefly (fixed + regression test). Mockups kept at docs/consultation-mockups.html.

**SI section = status table (Chris picked mockup B, 2026-08-03):** Instrument | Procedure | Status under the section-5 deadline table. items gained an `extra` JSON column (migration in db.init_db) carrying structured SI facts (procedure/act/laid/made/division/text_link/status); sis.status_line derives status from recorded events only (draft affirmative modelled: laid -> Commons approved -> awaiting Lords -> made; other procedures facts-only). Links: name -> statutoryinstruments.parliament.uk tracker, vote -> votes.parliament.uk, full text -> legislation.gov.uk (never JSON APIs; divisions.url is now the public page). Partner site: procedure chips with native-title hover explanations (partner.PROCEDURES glossary). Option D (group by parent Act) is the agreed growth path when multiple Acts' SIs arrive. Mockups in docs/si-mockups.html + docs/consultation-mockups.html.

**Closure freshness window (Chris, 2026-08-03):** closing entries render only while the terminal event is within `closure_fresh_days` (28, in settings.yaml) of the edition — run_weekly.split_stale_closures; stale (cold-start catch-up) closures are still record_closure'd so the once-ever guard holds, just never rendered. BoardRow gained closed_date (RA date / last sitting); Holyrood dates parsed from closed_note. The 2026-08-03 edition + partner site re-rendered without the four March-June closures; board now shows only the 5 live bills incl. 4254.

**Format v3 (Chris, 2026-08-03 evening): sections UNNUMBERED** — named headings in fixed order (Top lines, Week ahead, Votes, PQs, Consultations and calls for evidence, EDMs, Devolved, Statements, Active bills board, Secondary legislation, MP intelligence notes). Rationale: numbered headings implied gaps when empty sections were omitted. **Secondary legislation is its own section directly after the board** (SIs = legislation in stages, the board's sibling; not deadlines). digest.render_si(); partner MP regex tolerates numbered+unnumbered. July edition mechanically unnumbered. All references to "section N" in docs/memory are historical.

**MP intelligence live (2026-08-03 evening, Chris's V1 + rules):** section renamed "Parliamentarians on our issues" with standing subtitle declaring coverage; one merged line per member, kind chips (PQ/DEBATE/VOTE/EDM), cap 12 + overflow note; **NO watched/unwatched distinction** (Chris: interested in ALL parliamentarians); **votes never listed** (ledgered for profiles; division renders once in Votes section — flooding rule tested). Capture: sweep_pqs/sweep_edms write mp_events (dedup ux_mp_events on member+kind+ref) + resolve members; PQ items carry mp_refs. **Ledger precision gate: tier-1 or watchlist only** (first backfill let tier-2 answer-text noise in; wiped + re-run). Six-month backfill done twice: precise ledger = 361 events / 128 members (Feb 3-Jul 31). Leaderboard: McMurdock 43 (heavy migration PQ asker), Cleverly 26, Dodds/Lockhart/Snowden/Lowe 11. tools/backfill_mp_ledger.py (idempotent), tools/make_timelines.py -> docs/mp-timelines.html (internal; timeline = approved profile view). Section internal-only: partner MP_SECTION regex matches old+new titles. Opacity fixed same evening: intel.annotated_line appends '(re: matched term)' when the heading doesn't name it — 178 of 360 backfilled events needed it (third backfill run, annotated ledger final: 360 events / 129 members). Both capture paths annotate going forward.

**5CA defined + generator foundations (2026-08-04).** 5CA = Five Column Analysis (FACL/Kirk Shelley via CitizenGO Campaigns Brief Cheat Sheet, read from Chris's Google Docs): per decision-maker, stance gradient ++/+/0/-/-- with exactly one column marked, Target Y/N, Comments rationale; Evaluate phase records actual vote Y/N/0. Generator goal: pre-filled Plan-phase sheet for a bill — suggested placement + ledger evidence as Comments (spec in docs/5ca-notes.md). Key design fact: ledger measures ACTIVITY not STANCE; placement needs the stance layer (votes > speeches > EDM signatures > PQ framing; Claude text-scoring for the latter). Foundations built: (1) mp_events.areas (json area numbers; record_event is now an UPSERT refreshing line/areas/date on conflict — safe because mp_events has no editorial state; backfill re-runs retro-stamp), intel.area_activity()/area_names(); (2) EDM co-signatories: edms.fetch_sponsors (EarlyDayMotion/{id} detail, member details embedded so members cache seeds free), kind='edm-signed' for order>1 non-withdrawn, both capture paths; digest.BULK_KINDS = {vote, edm-signed} never render in weekly section (Chris's flooding rule). 172 tests. Remaining for generator: Sept divisions+Hansard, stance-classification pass, tools/make_5ca.py CSV matching Brief sheet columns.

**Stance layer + 5CA generator LIVE (2026-08-04 evening, commit 8e59527).** src/stance.py mirrors triage.py: live Claude pass (claude-sonnet-5, batch 20, max_tokens 4000 after a live truncation crash — salvage parser + per-batch storage now survive it) scores each ledger ref's full text -2..+2 vs CitizenGO positions; rubric guards: neutral information-seeking = 0, never infer from party; sponsors+signatories inherit motion's stance (shared ref). tools/score_stance.py recovers full question/motion text OFFLINE from data/raw (360/360 recovered, no refetching); idempotent. First run: ++15 +92 0×226 −27 −−1 (63% neutral = guard working). Classifier corrected MY misread: the Amnesty 'anti-rights movement' EDM CONDEMNS the report — signatories are ALLIES (+2), proving full-text beats title-guessing. tools/make_5ca.py <area>: Campaigns Brief columns (Decision-Maker, ++/+/0/-/--, Target Y/N, Comments) CSV to data/5ca/; strongest directional evidence wins (kind weight edm>edm-signed>pq, then recency), conflicts flagged never averaged, Target always blank (campaigner's call). First sheets: migration 62 decision-makers (++1 +10 0×24 −5 −−22 — the −−22 = signatories of the anti-enforcement UK-France pilot EDM), free speech 93. 182 tests. Votes/speeches (Sept) will outrank text stances via KIND_WEIGHT. **Full-roster mode (Chris, same evening, commit 060f9dc):** make_5ca defaults to ALL 649 sitting MPs (members.current_mp flag via tools/pull_commons_roster.py, ~33 API calls, re-run after by-elections); quiet MPs sit at 0 'No recorded activity'; peers excluded from full-roster sheets (no Commons vote) but kept in --active-only view. Roster pulled 2026-08-04 (Lab 362+43 co-op, Con 117, LD 71...). 184 tests.

**Historic backlog to 2020-01-01 approved (Chris, 2026-08-04): ALL sources incl. Hansard. Order: divisions -> historic PQ/EDM -> Hansard.** Taxonomy v0.4 (historic vocabulary): Online Safety Bill + Online Harms (A7), Nationality and Borders + Safety of Rwanda/Rwanda plan/scheme T1 + bare Rwanda T2 (A11), GRA reform + Gender Recognition Reform (A5), Keira Bell (A3), DIY abortion + early medical abortion T2 (A1); sweep terms extended; NEW division_sweep_terms list (bill-title vocabulary, 16 terms). Divisions capture built: search_commons/lords_divisions (paged, date-ranged) + fetch_*_breakdown (member lists embed party/seat -> cache seeds free); ledger kind='vote', **direction encoded in ref (div:c1798:aye vs :no) because stance is per-ref and Ayes/Noes are opposite**; KIND_WEIGHT vote=4 (top); rubric scores votes, procedural ping-pong titles -> 0; record_event commit=False batching (600-voter divisions); timelines count votes never list them. tools/backfill_divisions.py. **Pre-2024-parliament stamp: Chris chose Option A = NO STAMP** (dates lead every evidence line; mockups at docs/5ca-stamp-mockups.html, B=per-line tag and C=row banner rejected). 190 tests.

**Divisions backfill RUN (2026-08-04, commit 83fd0af): 245 matched divisions 2020-06->2026-07, 104,982 vote events, 0 gaps. Ledger: 105,422 events / 1,892 members; 641/649 sitting MPs have votes.** 491 vote refs stance-scored (158 directional; 333 procedural->0 as designed). Sign check passed: Kruger/Farron ++ vs Leadbeater -- on TIA third reading (free vote = gold signal). 5CA sheets transformed: migration 637/649 with evidence (was 56), assisted dying ++296/--326 from the TIA divisions. Caveats known: (a) migration sheet has 480 conflict flags - REAL mixed records (whipped ping-pong across two governments; Labour MPs opposed Tory enforcement bills then backed their own Border Security Bill); (b) whipped votes measure party line not conviction - free votes (TIA, abortion regs) are the gold signal; future refinement could down-weight whipped votes (Lords API exposes isWhipped; Commons doesn't). db 15MB, raw 8MB - committed fine. NEXT per Chris's ordering: historic PQ/EDM backfill to 2020 (year-windowed paging), then Hansard ingester.

**Editorial override layer (Chris's directives, 2026-08-04 late, commit 91ffd29):** (1) config/stance_overrides.yaml - org judgement OUTRANKS classifier: No on Illegal Migration/Safety of Rwanda/Nationality and Borders enforcement motions = -2, Aye = +2 (direction-safe motions only: readings + motions-to-disagree; wrecking/reasoned amendments + committee stand-parts excluded via unless_any); **Border Security Bill votes zeroed** ('don't give equal weighting' - it repealed the IMA/Rwanda scheme, so Labour ayes on it never offset anti-enforcement records; Cooper verified at --). apply_overrides idempotent, re-applied by every score_stance run; YAML gotcha: bare no: key parses as boolean False - normalised in load_overrides. Migration conflicts 480->105. (2) Whip annotations: Lords isWhipped from raw archive -> division_whip table (127 whipped/5 free); Commons conscience-convention free_vote_titles list (Terminally Ill Adults Bill, abortion); '; free vote'/'; whipped' shown in 5CA comments; free votes preferred at equal evidence strength. (3) **Always refer to the Terminally Ill Adults (End of Life) Bill in full - Chris: TIA is not a known abbreviation** (applies to all user-facing output incl. chat). 195 tests.

**Validation round 1 + Chris's rulings (2026-08-05).** Historic backlog complete: ledger 123,586 events / 1,915 members / 2020-01-07 onward (PQ+EDM year-windowed backfill 5,425+989; Hansard 13,792 speeches; 245 divisions/104,982 votes). Weekly pull now captures division breakdowns + Hansard too (was backfill-only; would have stopped growing 1 Sep) and scores its own week's stance refs, capped by settings.stance_weekly_max_refs=400 (~60p) with over-cap disclosed in gaps. Cost model: ~£5-6/yr ongoing (refs not volume: a 600-voter division = 2 refs). API needed because session scoring can't be unattended (Chris). **Rulings: (a) MIGRATION collated but EXCLUDED from 5CA** (excluded_from_5ca in stance_overrides.yaml; also retired the hardliner-inversion problem where Jenrick/Braverman scored -- for rebelling for a tougher bill). **(b) NO ministerial/whipped discount on conscience votes** - Kinnock stays -- for voting for assisted suicide; recorded as a deliberate non-feature. **(c) NI abortion regulations = -1 + `caps` mechanism** (ceiling +, upside-only, note in Comments): abortion -- went 229->12, 211 at -, good-record Conservatives no longer branded opponents. **(d) buffer zone* stays tier 1** + tools/clean_buffer_zone_noise.py (drop clearly-unrelated subjects only, keep ambiguous - deletion is irreversible and Hansard excerpts are ~900 chars; rescued 3 PCSC Bill clinic-buffer-zone speeches). 53 noise rows removed. **Stage 1 passage matching + area guards (2026-08-05, commits 1671d9c, 0ca6ac6).** filter.split_passages/match_passages/aggregate_passages: paragraphs (markup stripped, long ones split on sentences) filtered individually; areas from qualifying passages only; strongest passage stored as mp_events.excerpt and QUOTED in 5CA Comments (Kruger's row now quotes him instead of showing "Eating Disorder Awareness"). Both capture paths use it. tools/retag_passages.py (--kind debate|pq) rewrote 13,445 speech + 2,475 question rows offline, free, nothing deleted. **Found: I truncate evidence to 1500 chars but median speech is 3,100 (p90 8,800) - ~2,300 speeches were stance-scored on text that never contained their match. Stage 2 (send the matching passage; re-score only those, ~£3-4) NOT YET DONE.** **Diagnosis correction: the Diana Johnson knife-crime-on-abortion-sheet case is NOT long-speech cross-tagging - the watchlisted "Crime and Policing" entity carries area 1 (decriminalisation vehicle), so every mention of that omnibus bill inherits abortion. Needs a corroboration flag on broad watchlist entries; 6,344 debate rows carry area 1. STILL OPEN.** **Area guards (Chris ran all 5 suggested fixes): an issue area is now required to render in the weekly MP section AND timelines (5CA already filtered by area; the weekly section did NOT - 13 rows like "Spoke: Extreme Heat: Preparedness (re: Jim Shannon)" would have printed in Sunday's edition); stance skips area-less refs (139 had been paid for); parliamentarian names NO LONGER matching entities (Hansard prints names structurally; Chris's all-parliamentarians-equal position means the list confers nothing - now Watchlist.people, reference only); "Modern Service Framework" retired from processes (generic NHS vocab admitting dementia/cardiovascular questions). 287 area-less rows remain, render nowhere, never scored. Tests updated: organisation entities (SPUC) carry the watchlist floor, person names match nothing. 216 tests.**

**Slack LIVE (2026-08-05): token in secrets.yaml, bot `parlmonitor`, team CitizenGO, scopes chat:write + canvases:write confirmed via auth.test X-OAuth-Scopes.** SLACK_BOT_TOKEN + VERCEL_TOKEN both added as Actions secrets by Chris; full end-to-end Actions test run SUCCEEDED at his request (Slack post included - he explicitly wanted it as part of the test). Manual workflow_dispatch runs now default to dry_run=true (render+partner deploy, no Slack/Asana); NO_PUBLISH env / --no-publish flag on run_monday. **Scheduled Monday runs post to #campaigns-en-gb automatically from 10 Aug - flagged to Chris that the interim ask-first rule is now superseded by automation.** tools/post_5ca.py posts 5CA briefings as canvases (decision-relevant cut: shape of House, no-position target list with abstentionist/Sinn Fein/Speaker and devolved-seat members separated from genuine persuadables, marginals, conflicts, caveats); publish.slack_publish_canvas generalises the canvas dance. First one posted: assisted dying (canvas F0BMNFWN4DV) - 24 'no position' = only 9 genuine targets after non-voters excluded.

**BACKLOG COMPLETE 2026-08-05: 18,086 refs all scored (++2,286 / +3,352 / 0 8,550 / -2,744 / --1,154); ledger 123,533 events / 1,915 members / 2020-01-07 to 2026-08-04; all ten sheets + timelines regenerated.** Chris hit his API usage cap mid-tail (locked to 1 Sept, not a balance issue) then raised it by $8. **That exposed the one unprotected call in pull(): run_triage_pass was NOT wrapped, so a capped API would have crashed Sunday's unattended run after ingestion (no review file, Monday rendering untriaged items). Live triage now falls back to the deterministic stub with a disclosed gap.** Two more scorer resilience fixes: store_scores skips malformed stance values (a reply sent stance as a list); _salvage_objects decodes object-by-object because a max_tokens cut can land INSIDE a string, so the old last-'}' scan found one embedded in the broken value and threw, losing 39 whole batches; max_tokens 4000->8000 (long speech batches were truncating). 218 tests.

**Chris validates ALL 5CA placements (2026-08-05); may refine later; NO roadmap doc for now.** Vision list he's building (I suggested additions: close the Evaluate loop from division data, movement detection, opportunity ranking w/ RF4 rationale, co-signature coalition mapping, escalation alerts, campaign-vs-discards false-negative test, second-country readiness, official associations data (RMFI/APPG) before social media, provenance rule). Future phase per Chris: media/social/associations. **Cost reconciliation: ~£40 purchased, ~£30 consumed for ~13k refs; ~£8 was genuine waste from the pre-per-batch-storage truncation crash (my bug, can't recur); remaining backlog ~£6-8 (~250 batches). Steady state ~£7-8/yr all-in.**

## Resilience + Lords data quality (2026-08-11)
- Both weekly jobs are idempotent per week (`pull_log`, `publish_log` tables in the store, committed with run state) with retry cron slots that bypass the DST guard. `--force` re-runs. Never trust the Actions scheduler: it dropped Sunday's slot and ran Monday's 4h late in one weekend (9-10 Aug).
- Lords division events carry `amendmentMotionNotes` in their excerpt (ingest: `Division.notes`; backfill: [[tools/enrich_lords_divisions.py]]). Titles name the BILL, not the question - scoring from titles inverted amendment votes. 264 refs re-scored 11 Aug (~50p).
- Partner site PUBLIC (no password) until Monday 17 Aug publish restores the gate; middleware date-check backstop expires 18 Aug 23:00 UTC. X-Robots-Tag noindex permanent.
- OSA inquiry (Lords Comms & Digital, evidence closes 5pm 7 Sep) is the live ACT, owner Christopher: submission draft + committee briefing in session scratchpad; supporter case studies still needed.

## 5CA estate + scoring quality (2026-08-11/12)
- Full 5CA estate live for BOTH houses: sheets + matrix x {Commons 649, Lords 788}, tracker (Commons), per-area CSVs (`make_5ca.py [--peers]`). One roster tool pulls both houses (`current_mp` / `current_peer` flags).
- Confidence markers (strong/moderate/thin + reason, `stance.suggest_confidence`) are INTERNAL-ONLY: docs/ builds and CSVs carry them, partner builds ship blanked tier fields. Not on the 5CA tracker at all (Christopher).
- Excerpt-only scoring inverted direction on ~half of conflicted members' minority debate items (Lord Farmer flipped to -- by one misread). Fixed: 483 refs re-scored with full text; `_evidence_text()` now centres the window on the excerpt inside its surroundings permanently.
- Recency tie-break (newest of equal-magnitude items decides) kept as-is by Christopher's ruling ("ignore recency for now").
- Peers spot-check verdict (assisted dying): 0 inversions at the poles; the 0 column is NOT a persuadable universe - quiet supporters of assisted dying carry no ledger evidence (Purvis/Mallalieu/Dubs at 0-1 items). Lord Winston genuinely mixed, left flagged.

## Campaigns Briefs + EOS baseline (2026-08-13)
- `tools/make_briefs.py` auto-drafts Campaigns Briefs (strict template layout read from the template sheet) + filled 5CA tab CSV per subject; one brief per subject EVER (brief_log), --force regenerates. Wired into Monday run. Internal only.
- Supporter data comes from **Max** (`ai_campaigner` Slack agent, U0AGLTNSR4J): mention in #campaigns-en-gb (DMs disabled), replies in-thread in a stable pipe format in ~4 min. Lifetime baseline logged 2026-08-13: 366 campaigns; fundraising is SERIES grain from 2025-01-23 only, absence = not-available never zero.
- `tools/log_campaign_performance.py` parses Max -> campaign_performance + fundraising_series; keyword area map is explicit, unmapped listed not guessed (166 mostly out-of-taxonomy).
- RF#1 in briefs: top comparables + acquisition % (new/sigs) + topic rollup + baseline date; money only via explicit petition join. Christopher: individual campaign precedent > lifetime totals.
- Monthly chase is self-surfacing: baseline >35 days old -> make_briefs prints "stale" line -> Monday log relay shows it.
- parl-monitor Slack bot lacks channels:history scope; Christopher must add it for the pipeline to read Max unaided.

**2026-08-20/21, the Holyrood build (two days, feature-complete).** Scottish Parliament watching brief at parity with NI and richer in votes: `sp_*` tables, `tools/sp_pull.py` / `sp_divisions.py` / `sp_5ca.py` / `sp_monitor.py`, `config/sp_stance.yaml` (23 human-confirmed meaning lines: 12 placing incl. 3 one-sided, 8 NOT PLACEABLE across four documented failure modes, 4 motion sponsorship lines), `.github/workflows/sp-weekly.yml` (Fri 06:00+14:00 UTC). Key API facts in docs/api-notes.md: no search API (whole-year dumps, taxonomy classifies everything, NO sweep terms exist); `votesmotion` is singular-lowercase (the apilist's plural 404s); the motions endpoint silently ignores `?year=`; answers arrive inline on question rows; memberparties is native date ranges; TWO vote classes — motion divisions (per-MSP) and bill-amendment divisions (Official Report aggregates only, NO roll-call published anywhere, question CLOSED); the supports full-dump 503s but per-id works (fetch per matched motion). Year dumps fetch with `archive=False` (new HttpClient option); the DB is the archive; `--reclassify` re-tests offline. Tier gate: tier-2-only motions held not shown (congratulatory noise). Conflict reframe (Christopher): TARGET-SHAPED = chosen acts diverge from whipped votes = the persuadable pattern (first find: Colin Beattie, SNP). **ONE WRITER AT A TIME on data/parl-monitor.db** — two stance-scorer kills taught it; store_rows commits per 500, busy_timeout 120s. sp-weekly's day-one double miss was missing `permissions: contents: write` + sunset checkout@v4: copy ni-weekly, don't write workflows from memory. Westminster corrections same week: Law Commission guard (area 9 deflated 368→133 rows), RSE guard (Royal Society of Edinburgh), hyphen re-fetch (+5,433 debate events, a third of the ledger), full stance catch-up (~$26, approved, area 7 ++48→75). Migration renders in the Slack digest by decision 2026-08-21 (full inclusion) — do not "fix" as a leak.

**2026-08-21 evening, the Senedd build + a removal.** Wales at parity on questions and votes: `sd_*` tables, `tools/sd_pull.py` (ID-WALKING — no data API exists; /Search ignores its query; ModernGov XML exports serve HTML shells; per-id pages at record.senedd.wales/WrittenQuestion/<id> with a no-"Tabled on" miss signature), `sd_members.py` (roster from mySociety parlparse — 96 sitting post-expansion members with parties: Plaid 43, Reform 34, Lab 9; the Senedd's own party source WAF-403s our honest UA and **spoofing a browser is Christopher's decision, never a default**), `sd_divisions.py` (941 divisions / 58,698 per-member positions via the UNDOCUMENTED XMLExport found by reading mySociety's scraper; 'committee' param = parliament id, 700=Sixth 908=Seventh; row wrapper EMBEDS the parliament name — XML_Plenary-SixthSenedd_Vote — and the division key is Contribution_ID, <ID> is member-level), `sd_monitor.py`, `sd-weekly.yml` (Thursday, sed-derived from ni-weekly). Holdings: 9,609 questions to Jan 2024 (320 ours; education dominates Wales at 105), AD member debate DEFEATED 19-26 Oct 2024, four TIA LCM divisions Feb 2026, Cass debate quartet May 2024. No sd_stance.yaml yet; classification by DEBATE TITLE (coarser than Holyrood, said inline). **CROSS-CHAMBER DOSSIERS REMOVED by Christopher's decision 2026-08-21 ("I'm not sure cross-chamber dossiers do anything helpful. remove and stop suggesting") — tools/make_dossier.py deleted; never rebuild or re-suggest.** sp-weekly PROVEN (delayed day-one run executed the fixed workflow end-to-end). Shell PATH broke mid-session on this machine: prefix PATH=/usr/bin:/bin per command if commands go missing.

**2026-08-22: Senedd 5CA + stance file (shortlist item 3 done, commit 6c2f135).** `tools/sd_5ca.py` + `config/sd_stance.yaml`: ONE placeable division — key 623756, the Oct 2024 assisted dying member debate (rejected 19-26 on a free vote; transcript-verified: Julie Morgan moved the PRO-AD motion with Evans/Fychan/Price, tied to the Leadbeater Bill; For -1 / Against +1). All four TIA LCM divisions NOT PLACEABLE (movers disclaimed the moral question; splits are constitutional coalitions — Mabon ap Gwynfor voted AGAINST AD in 2024 yet moved the LCM amendments, the proof in one person). Sheet: 19 of 96 sitting Members place (+12/−7, the − column = surviving co-sponsors, checks out). Name join: NFKD accent-strip + first+last-token fallback refusing ambiguity; Record-vs-parlparse misses documented (parlparse holds Eluned Morgan as malformed "Mair Eluned"; the Llywydd is an office name). Most unmatched voters are FORMER Members (area 2's divisions predate the May 2026 election) — reported as a separate bucket, not a join failure. Monitor honesty line computes stance counts live from the yaml. 665 tests. Remaining shortlist: 4 Holyrood committees, 5 devolved consultations.

**2026-08-22 later: shortlist items 4+5 done (commits 60a8453, c1c423d), ranked shortlist COMPLETE.** (4) Holyrood committee scrutiny: `Orscommitteemeeting?year=N` works for 2025/2026 despite the apilist stopping at 2024; tools/sp_committees.py → sp_events under `occ` keys ("Committee -- item" headings; classification on item heading only, never the room's name; witnesses = null Person, skipped; the API intermittently interleaves error strings — parsers skip non-dicts). Backfill: 2,606 events/14 committees, 1,598 = Health Committee's Assisted Dying Bill scrutiny. (5) Devolved government consultations: dg_consultations table + tools/dg_consultations.py; consult.gov.scot = Citizen Space with JSON API DISABLED (finder page scraped, honest UA ok); consultations.nidirect.gov.uk same markup (instance hosts staff surveys/crochet clubs — classification sifts, nothing suppressed); **www.gov.wales browser UA authorised 2026-08-22** (second host_user_agents entry; CloudFront 403s all non-browser UAs); gov.wales `?status=open` is SILENTLY IGNORED — real param `field_consultation_status=1`, includes "Open call for evidence". Closing dates only on detail pages, fetched once per new key, all archive=False. Each weekly pulls its own nation (one writer). Monitors gained GOVERNMENT CONSULTATIONS sections via devolved.render_consultations. First pull 63 open/1 ours. **Flagged taxonomy gaps, not added: "religious education" (NI RE Core Syllabus consultation missed) and VAWG vocabulary (Scotland women-and-girls justice consultation missed) — Christopher's call.** **DB AT 88MB, GitHub hard-fails at 100MB — options flagged (LFS / compress bodies / split file), no action taken.** 676 tests.

**2026-08-22 taxonomy v1.0 (commit c0992de): "religious education" area 6 tier 1** (Christopher's instruction; measured first, all samples on-topic). The term uncovered **SP Bill 66 — Children (Withdrawal from Religious Education and Amendment of UNCRC Compatibility Duty) (Scotland) Bill, PASSED Stage 3 66-51 on 2026-02-17**: 3 per-MSP roll-call stage votes (S6M-19866/S6M-20099/S6M-20813), 24 amendment divisions, 491 committee contributions — all reclassified offline; NO meaning lines written (read-first: the bill's parental-rights direction needs the text — S6M-20813 is the prime meaning-line candidate when Christopher wants it). NI RE Core Syllabus consultation closes 2026-09-30 (the prompt for the term). **Taxonomy v1.1 same day (commit 2b0325e): VAWG vocabulary at area 5 tier 2** ("violence against women and girls" + VAWG + VAWDASV, the Welsh statutory frame; Equality Act logic — mostly strategy plumbing, triage gates it, the definition-of-woman slice scores). The Scottish women-and-girls justice consultation now marks OURS — **closes 2026-08-31, 9 days at add time**. S6M-19970.2 read and CONFIRMED same day (commit ae8d123): aye +1 / no −1, the S6M-18016.1 shape — insert-at-end (Tess White, Con), operative content ours twice over (FWS sex-definition compliance + grooming gangs inquiry call), Labour's 20 abstentions repeat the alignment tell, aye lobby beyond the whip was Ewing/Regan/Balfour choosing it (all four named members left at the 2026 election — the line places sitting no-voters and stands as record). sp_stance now 23 division entries. **Meaning lines confirmed same day (commit aa66c47): S6M-20813 Stage 3 ±2 (the Bill narrows the s.9 parental conscience clause — RME withdrawal removed, observance withdrawal subject to child's opt-back-in, UNCRC duty extended; Kerr's speeches + FWS briefing are the read evidence), S6M-19866 Stage 1 ±1, S6M-20099 FR one-sided no+1 (cross-tab reproduced S6M-19456 exactly: 27 FR-noes all S3-no/absent, 17 FR-ayes were Labour S3 opponents). Green S3 abstention is NOT friendly (they wanted an independent child opt-out). sp_stance now 22 division entries; area 6 sheet 63/129 placed.** Westminster historic speeches under old vocabulary stay uncaptured unless a backfill re-runs. 679 tests.

**2026-08-24: Senedd committees + forward look (shortlist items 4+5, commit defae6a) — SHORTLIST NOW FULLY COMPLETE.** **Plenary IS ModernGov committee 908** — the votes exporter's `committee=` param was never a parliament id (700/908 = Sixth/Seventh Senedd PLENARY committee ids), so the XMLExport index serves committee sittings identically. **Bug found: the transcript wrapper embeds the VENUE** (`XML_HealthAndSocialCareCommittee_English` vs `XML_Plenary-SixthSenedd_English`), so the Plenary-only regex silently returned ZERO for every committee; now backreferenced `<(XML_[^>\s]*_English)>...</\1>`. THIRD time a Senedd wrapper name in a regex has bitten — assume it varies. tools/sd_committees.py: 19 committees (Plenary + Welsh Youth Parliament excluded — WYP members aren't MSs), sdcc keys in sd_events, new sd_committees table. **Forward look = committee detail page PROSE** ("will next meet on Thursday 17 September"), year inferred forward never backwards; future agendas publish later so a forward row has a date and NO subject. **ModernGov calendar is a dead end** — all three views exhibitions only, Month/Year silently ignored (Holyrood /api/events shape). Agenda pages carry no parseable items either; items come from transcript `Agenda_item_english`. First pull: 22 sittings, 7 September dates, 1 matched event — **Petitions Committee P-07-1564 "Restore parental consent for RVE lessons"**: the Curriculum and Assessment (Wales) Act 2021 removed Welsh parents' RVE withdrawal right INCLUDING at faith schools. Ledger thin BY AGE (committees formed mid-2026). **Cross-nation pattern now visible: Wales lost the RVE withdrawal right (2021), Scotland narrowed it (Feb 2026 Act), NI's RE syllabus consultation closes 30 Sep — flagged to Christopher in Slack.** 698 tests.

**2026-08-21 late: EDITORIAL LOOP RETIRED (Christopher's decision, verbatim: "I want the editorial loop act, watch, note removed from the westminster version. I'll create asana tasks myself based on what the monitor surfaces and automatic briefs it produces.").** Rendering is score-driven (triage_score 3 = top line, >=2 = section); no review files, no ACT/WATCH/NOTE, no owners, no Asana reading task. make_briefs selects score-3 subjects; briefs auto-upload to Drive. **The no-Asana-tasks half of this ruling was SUPERSEDED 2026-08-31 (Christopher, third asking): every generated brief creates a native approval task in project 1211423235936092 (EN GB Weekly Meeting agenda), check_brief_approvals is live again, and an APPROVED verdict raises a "Refine and submit to PPAE" follow-up task (once per brief, brief_log.followup_gid; sweep retries failures).** The ACT/WATCH/NOTE editorial-loop half stays retired. The nine pending briefs generated during the task-less window were retired outright on 2026-08-31 (marked rejected, files archived, sheets binned) — no backfill, per Christopher.

**Party splits are deliberately unrendered (Christopher, 2026-08-31).** Free
votes have no party line, so with/against/splitting are the wrong grammar --
`relativeToParty` prints only genuine rebellion (whipped AND against the
majority; zero real cases in the corpus, synthetic-rebel test keeps the
mechanism honest). Four rendering options for free-vote splits were mocked
(nothing / tally tooltip / "Lab split 224-160" / "one of 160 Lab voting No");
he chose "do nothing for now" -- the recommendation on the table if it
reopens is the tally tooltip, with the party-fact line as the mobile
fallback. The splits ship in the page data either way, reserved for 5CA
analysis of whipped divisions only.
